<% layout("/layouts/boilerplate") %>
  <main class="p-4">
    <div
      class="container mt-10 mx-auto p-4 flex flex-col justify-center items-center gap-10 bg-red-500 text-red-200 rounded">
      <header class="flex flex-col gap-2">
        <h2 class="mb-2 text-4xl text-center font-semibold">Womp womp...</h2>
        <h3 class="text-4xl text-center font-semibold">
          <%= err.message %>
        </h3>
        <p class="text-4xl text-center font-semibold">:/</p>
      </header>
      <p class="font-bold">
        <%= err.stack %>
      </p>
    </div>
  </main>